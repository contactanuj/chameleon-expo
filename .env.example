# Copy to .env for local EAS builds (the real .env is gitignored).
# Create a token at https://expo.dev/accounts/[account]/settings/access-tokens
# In CI, set this as a GitHub Actions secret named EXPO_TOKEN instead — do NOT commit it.
EXPO_TOKEN=
